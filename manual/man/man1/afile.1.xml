<?xml version="1.0" encoding="UTF-8"?>
<refentry id="afile.1">
  <refmeta>
    <refentrytitle>afile</refentrytitle>

    <manvolnum>1</manvolnum>

    <refmiscinfo class="date">26 Feb 1998</refmiscinfo>

    <refmiscinfo class="source">:NETATALK_VERSION:</refmiscinfo>
  </refmeta>

  <refnamediv>
    <refname>afile</refname>

    <refpurpose>display type and creator of Apple Macintosh files (netatalk
    format)</refpurpose>
  </refnamediv>

  <refsynopsisdiv>
    <cmdsynopsis>
      <command>afile</command>

      <arg choice="opt">-a</arg>

      <arg choice="plain" rep="repeat"><replaceable>file</replaceable></arg>

      <sbr />
    </cmdsynopsis>
  </refsynopsisdiv>

  <refsect1>
    <title>DESCRIPTION</title>

    <para><command>afile</command> displays the name and Macintosh type and
    creator of the <emphasis remap="I">file</emphasis> arguments. Tests
    whether the file is an AppleDouble header, in which case it checks the
    corresponding data fork exists, or assumes it is a data fork in which case
    it looks for the corresponding AppleDouble to find the type/creator
    information.</para>

    <para><command>afile</command> does not look at any of the extension
    mapping files such as AppleVolumes.system.</para>
  </refsect1>

  <refsect1>
    <title>OPTIONS</title>

    <variablelist remap="TP">
      <varlistentry>
        <term><option>-a</option></term>

        <listitem>
          <para>Include directories and data files of unknown type (i.e.
          without corresponding AppleDouble) in output.</para>
        </listitem>
      </varlistentry>
    </variablelist>
  </refsect1>

  <refsect1>
    <title>DIAGNOSTICS</title>

    <para>returns exit status 0 if all files have a corresponding valid
    .AppleDouble header or data fork, or 99 for bad command line options.
    Otherwise it returns the following error code relating to the last invalid
    file.</para>

    <para>1 file doesn&#39;t exist</para>

    <para>2 file is unreadable</para>

    <para>3 file is directory</para>

    <para>4 file is AppleDouble without data fork</para>

    <para>5 file is AppleDouble with unreadable data fork</para>

    <para>6 file is data fork without AppleDouble</para>

    <para>7 file is data fork with unreadable AppleDouble</para>

    <para>8 file is data fork with short AppleDouble</para>

    <para>9 bad magic in AppleDouble</para>
  </refsect1>

  <refsect1>
    <title>SEE ALSO</title>

    <para><citerefentry><refentrytitle>achfile</refentrytitle><manvolnum>1</manvolnum></citerefentry>,
    <citerefentry><refentrytitle>afpd</refentrytitle><manvolnum>1</manvolnum></citerefentry></para>
  </refsect1>
</refentry>
